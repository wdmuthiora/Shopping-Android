<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moringaschool.shopping">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shopping">
        <activity
            android:name=".Activity.Activity.CartListActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.Activity.ShowDetailActivity"
            android:exported="false" />
        <activity android:name=".Activity.Activity.IntroActivity" />
        <activity
            android:name=".Activity.Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>